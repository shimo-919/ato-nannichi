import {useState} from 'react'
import Modal from './Modal'
import DeadlinePicker from './DeadlinePicker'
const markers=['🌱','🚶','🐣','⭐','🚀','🐻','🐱','⚽','🎸','📘','💪','💰']
export default function GoalForm({goal,onSave,onClose}){const[title,setTitle]=useState(goal?.title||''),[deadline,setDeadline]=useState(goal?.deadline||''),[progressMarker,setProgressMarker]=useState(goal?.progressMarker||'🌱');const submit=e=>{e.preventDefault();if(!title.trim()||!deadline)return;onSave({...goal,title:title.trim(),deadline,progressMarker});onClose()};return <Modal title={goal?'目標を編集':'目標を追加'} onClose={onClose}><form onSubmit={submit} className="form-stack"><label>目標名<input autoFocus value={title} onChange={e=>setTitle(e.target.value)} placeholder="いつまでに何を達成する？" required/></label><DeadlinePicker value={deadline} onChange={setDeadline} kind="goal"/><div><span className="field-label">進行マーカー</span><div className="marker-picker">{markers.map(marker=><button type="button" key={marker} className={progressMarker===marker?'selected':''} onClick={()=>setProgressMarker(marker)} aria-label={`${marker}を選ぶ`}>{marker}</button>)}</div></div><button className="primary-button" type="submit" disabled={!deadline}>保存する</button></form></Modal>}
