import { habitCompletedOn, recurrenceLabel, weeklyCount } from '../data'
export default function HabitRow({ habit, records, day, onToggle }) { const done=habitCompletedOn(records,habit.id,day); const weekly=habit.recurrence==='weekly'; return <div className={'habit-row '+(done?'done':'')}><div><strong>{habit.name}</strong><small>{weekly?`今週 ${weeklyCount(records,habit.id,day)} / ${habit.weeklyTarget} 回`:recurrenceLabel(habit)}</small></div><button className={'stamp-button '+(done?'stamped':'')} onClick={()=>onToggle(habit.id,day)} aria-label={done?'完了を取り消す':'完了にする'}>{done?'✦':''}</button></div> }
