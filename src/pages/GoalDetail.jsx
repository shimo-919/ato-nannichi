import {dateKey,daysLeft,deadlineState,deadlineText,formatDate,habitCompletedOn,isHabitScheduled,recurrenceLabel,sortMilestones} from '../data'
import GoalPhotos from '../components/GoalPhotos'
export default function GoalDetail({goal,milestones,habits,habitRecords,toggleMilestone,onToggleHabit,onSaveGoal,onBack,onEdit,onDelete,onAddMilestone,onEditMilestone,onAddHabit,onEditHabit,onDeleteHabit}){const journey=sortMilestones(milestones.filter(m=>m.goalId===goal.id)),completed=journey.filter(m=>m.completedAt).length,next=journey.find(m=>!m.completedAt),goalLeft=daysLeft(goal.deadline),today=dateKey(),linked=habits.filter(h=>h.goalId===goal.id);return <div className="page goal-detail-page"><button className="back-button" onClick={onBack}>← 目標一覧</button><header className="detail-hero"><span className="eyebrow">GOAL</span><h1>{goal.title}</h1><p>期限　{formatDate(goal.deadline)}</p><div className={'countdown huge '+deadlineState(goalLeft)}>{deadlineText(goalLeft)}</div><div className="detail-achievement">小目標 <strong>{completed} / {journey.length}</strong> 達成</div></header><GoalPhotos goal={goal} onSave={onSaveGoal}/>{next&&<section className="featured-next"><span>次の小目標</span><strong>{next.content}</strong><div><small>{formatDate(next.deadline)}</small><b className={deadlineState(daysLeft(next.deadline))}>{deadlineText(daysLeft(next.deadline))}</b></div></section>}<section className="section journey-section"><div className="section-heading"><div><span className="eyebrow">MILESTONES</span><h2>小目標の道のり</h2></div><button className="text-button" onClick={onAddMilestone}>＋ 追加</button></div>{journey.length?<div className="milestone-journey">{journey.map(m=>{const done=Boolean(m.completedAt),isNext=next?.id===m.id,left=daysLeft(m.deadline),overdue=!done&&left<0;return <article className={`journey-milestone ${done?'completed':''} ${isNext?'next':''} ${overdue?'overdue':''}`} key={m.id}><button className="journey-dot" onClick={()=>toggleMilestone(m.id)}>{done?'✦':''}</button><div className="journey-content">{isNext&&<span className="next-badge">NEXT</span>}<button className="milestone-edit" onClick={()=>onEditMilestone(m)}>編集</button><strong>{m.content}</strong>{done?<p className="completed-date">{formatDate(m.completedAt.slice(0,10))}に達成</p>:<><span className="journey-deadline">期限　{formatDate(m.deadline)}</span><div className="journey-days"><b className={deadlineState(left)}>{deadlineText(left)}</b>{overdue&&<small>期限を過ぎています</small>}</div></>}</div></article>})}</div>:<div className="empty">小目標を追加すると、ここに道のりができます。</div>}</section><section className="section goal-habits"><div className="section-heading"><div><span className="eyebrow">ROUTINES</span><h2>この目標の継続行動</h2></div><button className="text-button" onClick={onAddHabit}>＋ 追加</button></div>{linked.length?<div className="goal-habit-list">{linked.map(h=>{const scheduled=isHabitScheduled(h,today),done=habitCompletedOn(habitRecords,h.id,today);return <article className="goal-habit-row" key={h.id}><div><strong>{h.name}</strong><span>{recurrenceLabel(h)}</span></div><div className="habit-actions"><button className="small-action" onClick={()=>onEditHabit(h)}>編集</button><button className="small-action danger-text" onClick={()=>onDeleteHabit(h)}>削除</button>{scheduled&&<button className={'stamp-button '+(done?'stamped':'')} onClick={()=>onToggleHabit(h.id,today)}>{done?'✦':''}</button>}</div></article>})}</div>:<div className="empty">この目標の継続行動はまだありません。</div>}</section><div className="danger-zone"><button onClick={onEdit}>目標を編集</button><button className="danger" onClick={onDelete}>目標を削除</button></div></div>}
