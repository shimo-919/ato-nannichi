import { daysLeft, deadlineState, deadlineText, formatDate } from '../data'
export default function Goals({goals,milestones,onOpenGoal,onAdd}) { return <div className="page"><header className="page-header row"><div><p>GOALS</p><h1>目標</h1></div><button className="primary-button compact" onClick={onAdd}>＋ 追加</button></header><div className="goal-list">{goals.map(g=>{const ms=milestones.filter(m=>m.goalId===g.id); return <button className="goal-card" key={g.id} onClick={()=>onOpenGoal(g.id)}><div className="goal-card-top"><div><h3>{g.title}</h3><small>期限：{formatDate(g.deadline)}</small></div><span>›</span></div><div className={'countdown '+deadlineState(daysLeft(g.deadline))}>{deadlineText(daysLeft(g.deadline))}</div><div className="goal-meta"><span>小目標 {ms.filter(m=>m.completedAt).length} / {ms.length} 完了</span></div></button>})}{!goals.length&&<div className="empty">目標を追加すると、ここに見通しが並びます。</div>}</div></div> }
